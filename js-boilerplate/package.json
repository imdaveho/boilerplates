{
  "name": "${project_name}",
  "version": "0.0.1",
  "description": "${project_description}",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "${author}",
  "license": "${license}",
  "devDependencies": {
    "gulp": "^3.9.0",
    "jspm": "^0.16.19"
  },
  "dependencies": {}
}
